import 'package:flutter/material.dart';

class UpdateProfileUI extends StatelessWidget {
  const UpdateProfileUI({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("UpdateProfileUI"),
    );
  }
}
